"use client";

import { motion } from "framer-motion";
import { ArrowRight, BarChart3, Cpu, Gauge, GitBranch, HardDrive, Layers3, PiggyBank, ScanSearch, ShieldCheck, Zap } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import type { ReactNode } from "react";
import FAQ, { type FaqItem } from "@/components/FAQ";

const layers = [
  ["01", "Idle Allocation", "GPUs get reserved for a job, and the reservation often outlives the job itself. An instance sitting allocated but not actively computing is the single biggest source of waste in most GPU environments, and it's invisible unless something is watching utilization in real time.", HardDrive],
  ["02", "Over-Provisioned Inference", "It's common to see eight GPUs serving inference traffic that two could comfortably handle, because nobody's gone back to check since the initial deployment. Inference workloads rarely get resized after launch the way training jobs get reviewed before a big run.", Layers3],
  ["03", "Wrong Hardware for the Job", "Training and inference have fundamentally different hardware needs - A100s and H100s make sense for large training runs, while L4 or L40S instances are often more cost-effective for inference. Using training-grade hardware for inference workloads is a common and expensive mismatch.", Cpu],
  ["04", "Uncaptured Discount Opportunities", "Reserved capacity and committed-use pricing can cut 30 to 50% off baseline GPU costs, but committing requires confidence in your baseline usage - confidence most teams don't have without granular utilization data to back it up.", PiggyBank],
] as const;

const responses = [
  ["Continuous Idle Detection", "Zolix monitors GPU utilization in real time and flags instances allocated but not actively computing for extended periods - the kind of waste that typically goes unnoticed until a monthly bill review, if it's noticed at all.", ScanSearch],
  ["Inference Rightsizing", "Zolix compares your current inference GPU allocation against actual request volume and latency requirements, flagging over-provisioned deployments and recommending a sizing that matches real traffic rather than a launch-day estimate.", Gauge],
  ["Training vs. Inference Cost Separation", "Because these two workload types have completely different cost profiles and optimization strategies, Zolix tracks them separately from the start - so a spike in one doesn't get lost inside a single blended GPU line item.", GitBranch],
  ["Hardware Fit Recommendations", "Zolix flags workloads running on hardware that doesn't match their actual compute pattern - training-grade GPUs serving inference traffic, or inference-optimized instances straining under a training job they weren't sized for.", Cpu],
  ["Commitment Coverage Analysis", "Zolix analyzes your actual GPU usage baseline over time and recommends where Reserved Instance or committed-use coverage would pay off - backed by real utilization data, not a guess about future usage.", BarChart3],
] as const;

function Button({ href, children, light = false }: { href: string; children: ReactNode; light?: boolean }) {
  const className = `group inline-flex items-center gap-3 rounded-full px-7 py-4 text-[10px] font-bold uppercase tracking-[0.18em] transition-colors ${light ? "bg-white text-zolix-dark hover:bg-zolix-orange hover:text-white" : "bg-zolix-dark text-white hover:bg-zolix-orange"}`;
  const content = <>{children}<ArrowRight size={15} strokeWidth={3} className="transition-transform duration-200 group-hover:translate-x-1" /></>;
  return href.startsWith("http") ? <a href={href} target="_blank" rel="noreferrer" className={className}>{content}</a> : <Link href={href} className={className}>{content}</Link>;
}

const reveal = { initial: { opacity: 0, y: 16 }, whileInView: { opacity: 1, y: 0 }, viewport: { once: true, amount: 0.18 } };

export default function GpuInfrastructureTechnology({ faqs, breadcrumbs }: { faqs: FaqItem[]; breadcrumbs: ReactNode }) {
  return <>
    <section data-nav-theme="beige" className="relative overflow-hidden bg-zolix-beige px-6 pb-20 pt-40 md:pb-28">
      <div className="absolute -right-24 top-10 h-[34rem] w-[34rem] rounded-full border border-zolix-orange/15" />
      <div className="absolute right-32 top-48 h-60 w-60 rounded-full bg-zolix-orange/15 blur-3xl" />
      <div className="relative mx-auto max-w-7xl">
        {breadcrumbs}
        <div className="grid gap-14 lg:grid-cols-[1.05fr_.95fr] lg:items-center">
          <div>
            <motion.p initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} className="mb-7 text-[10px] font-bold uppercase tracking-[0.22em] text-zolix-orange">Technology / GPU Infrastructure</motion.p>
            <motion.h1 initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.55 }} className="max-w-3xl text-5xl font-extrabold leading-[.94] tracking-[-0.06em] text-zolix-dark md:text-7xl">Cloud Cost Optimization Solutions for GPU Infrastructure</motion.h1>
            <motion.p initial={{ opacity: 0, y: 14 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.12 }} className="mt-8 max-w-2xl text-lg font-medium leading-relaxed text-zolix-dark/60 md:text-xl">A single H100 instance can run $30 to $50 an hour on demand - 60 to 100 times the cost of a standard compute instance. Zolix tracks GPU utilization continuously and tells you exactly where that spend is going, without installing anything inside your training or inference environment.</motion.p>
            <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.2 }} className="mt-10 flex flex-wrap gap-4"><Button href="/demo">Book a Demo</Button></motion.div>
          </div>
          <motion.div initial={{ opacity: 0, x: 16 }} animate={{ opacity: 1, x: 0 }} transition={{ duration: 0.65, delay: 0.1 }} className="relative mx-auto w-full max-w-lg rounded-[2rem] border border-zolix-dark/10 bg-white/70 p-6 shadow-[0_24px_70px_rgba(26,26,26,0.08)] backdrop-blur-sm md:p-8">
            <div className="flex items-center justify-between border-b border-zolix-dark/10 pb-5 text-[9px] font-bold uppercase tracking-[0.18em]"><span className="text-zolix-dark/45">GPU utilization</span><span className="flex items-center gap-2 text-zolix-orange"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-zolix-orange" />Real-time</span></div>
            <div className="mt-7 space-y-3">
              {[["Idle allocation", "Reserved but not computing", HardDrive], ["Wrong hardware fit", "Training GPUs serving inference", Cpu], ["Discount coverage", "Reserved vs. on-demand baseline", PiggyBank]].map(([title, label, Icon], index) => {
                const NodeIcon = Icon as typeof HardDrive;
                return <motion.div key={title as string} initial={{ opacity: 0, x: 8 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: 0.32 + index * 0.1 }} className="relative flex items-center gap-4 rounded-2xl bg-zolix-beige/70 p-4"><span className="relative z-10 grid h-7 w-7 place-items-center rounded-full bg-zolix-dark text-white"><NodeIcon size={13} /></span><div><p className="text-sm font-bold text-zolix-dark">{title as string}</p><p className="mt-0.5 text-xs font-medium text-zolix-dark/45">{label as string}</p></div><span className="ml-auto text-[9px] font-bold uppercase tracking-wider text-zolix-orange">Flagged</span></motion.div>;
              })}
            </div>
            <p className="mt-7 border-t border-zolix-dark/10 pt-5 text-sm font-medium leading-relaxed text-zolix-dark/50">Average GPU utilization across production Kubernetes environments runs as low as 5%.</p>
          </motion.div>
        </div>
        <div className="mt-16 grid gap-3 border-t border-zolix-dark/10 pt-6 sm:grid-cols-3"><p className="flex items-center gap-3 text-xs font-bold text-zolix-dark/65"><ShieldCheck size={16} className="text-zolix-orange" />Read-only IAM roles</p><p className="flex items-center gap-3 text-xs font-bold text-zolix-dark/65"><GitBranch size={16} className="text-zolix-orange" />Training vs. inference split</p><p className="flex items-center gap-3 text-xs font-bold text-zolix-dark/65"><Zap size={16} className="text-zolix-orange" />Multi-cloud pricing compare</p></div>
      </div>
    </section>

    <section data-nav-theme="light" className="bg-white px-6 py-20 md:py-24"><div className="mx-auto max-w-7xl"><p className="mb-10 text-[10px] font-bold uppercase tracking-[0.22em] text-zolix-orange">The Scale of the Problem</p><div className="grid gap-10 border-t border-zolix-dark/10 pt-9 md:grid-cols-3"><div><p className="text-4xl font-extrabold tracking-[-0.06em] text-zolix-dark md:text-5xl">5%</p><p className="mt-4 max-w-xs text-sm font-medium leading-relaxed text-zolix-dark/55">average GPU utilization reported across many production Kubernetes environments, meaning most teams are paying for capacity they almost never touch.</p></div><div><p className="text-4xl font-extrabold tracking-[-0.06em] text-zolix-dark md:text-5xl">55-80%</p><p className="mt-4 max-w-xs text-sm font-medium leading-relaxed text-zolix-dark/55">the share of enterprise AI GPU spend now going to inference rather than training, a shift from just a few years ago.</p></div><div><p className="text-4xl font-extrabold tracking-[-0.06em] text-zolix-dark md:text-5xl">20-40%</p><p className="mt-4 max-w-xs text-sm font-medium leading-relaxed text-zolix-dark/55">how much ancillary costs like storage, data movement, and experiment tracking typically add on top of raw GPU compute spend.</p></div></div></div></section>

    <section data-nav-theme="beige" className="bg-zolix-beige px-6 py-24 md:py-32"><div className="mx-auto max-w-7xl"><div className="grid gap-12 lg:grid-cols-2 lg:items-start"><div><h2 className="text-4xl font-bold leading-[1.05] tracking-[-0.05em] text-zolix-dark md:text-6xl">The Four Layers Where GPU Spend Actually Hides</h2><p className="mt-6 max-w-xl text-lg font-medium leading-relaxed text-zolix-dark/55">GPU spend is shaped by more than the accelerator itself. Zolix connects the layers around compute so teams can see where capacity, data, and platform choices are creating cost.</p><div className="mt-8"><Button href="/demo">Book a Demo</Button></div></div><motion.div {...reveal} className="relative min-h-[26rem] overflow-hidden rounded-2xl bg-zolix-dark shadow-[0_18px_45px_rgba(155,85,0,.16)]"><Image src="https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&w=1400&q=85" alt="Computing hardware representing GPU infrastructure" fill sizes="(min-width: 1024px) 45vw, 100vw" className="object-cover" /><div className="absolute inset-0 bg-gradient-to-r from-zolix-dark/90 via-zolix-dark/35 to-transparent" /><div className="absolute inset-x-5 bottom-5 flex items-end justify-between"><span className="rounded-full border border-white/20 bg-zolix-dark/55 px-3 py-2 text-[9px] font-bold uppercase tracking-[.16em] text-white backdrop-blur-md">GPU cost intelligence</span><a href="https://unsplash.com/license" target="_blank" rel="noreferrer" className="text-[7px] font-medium text-white/60 underline-offset-2 hover:text-white hover:underline">Photo licensed via Unsplash</a></div></motion.div></div><div className="mt-20 divide-y divide-zolix-dark/10 md:mt-28">{layers.map(([number, title, desc, Icon], index) => <motion.article key={number} {...reveal} transition={{ delay: index * 0.04 }} className="group flex items-center gap-6 rounded-2xl px-4 py-8 transition-all duration-300 hover:bg-white/70 md:gap-8 md:px-6 md:py-10"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-white text-xs font-bold tracking-widest text-zolix-orange transition-transform duration-300 group-hover:-translate-y-0.5">{number}</span><div className="flex-1"><h3 className="text-xl font-bold tracking-tight text-zolix-dark md:text-2xl">{title}</h3><p className="mt-4 max-w-2xl text-[15px] font-medium leading-relaxed text-zolix-dark/55">{desc}</p></div><Icon size={20} className="hidden shrink-0 text-zolix-orange opacity-0 transition-all duration-300 group-hover:translate-x-1 group-hover:opacity-100 md:block" /></motion.article>)}</div><div className="mt-16 rounded-3xl bg-zolix-dark px-8 py-14 text-center text-white md:px-16 md:py-16"><h3 className="text-3xl font-bold leading-none tracking-[-0.05em] md:text-5xl">Ready to Find Out Where Your GPU Spend Is Hiding?</h3><div className="mt-8 flex flex-wrap justify-center gap-4"><Button href="https://lite.zolix.ai/signup" light>Get Started</Button><Button href="/demo" light>Book a Demo</Button></div></div></div></section>

    <section data-nav-theme="light" className="bg-white px-6 py-24 md:py-32"><div className="mx-auto max-w-7xl"><div className="max-w-3xl"><p className="mb-5 text-[10px] font-bold uppercase tracking-[0.22em] text-zolix-orange">How Zolix Responds to Each Layer</p><h2 className="text-4xl font-bold leading-none tracking-[-0.05em] text-zolix-dark md:text-6xl">A response built for every layer of GPU spend</h2></div><div className="mt-16 grid gap-x-14 gap-y-10 md:grid-cols-2">{responses.map(([title, desc, Icon], index) => <motion.article key={title} {...reveal} transition={{ delay: (index % 2) * 0.06 }} className="group border-t border-zolix-dark/15 pt-6"><div className="flex items-start gap-5"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-zolix-beige text-zolix-orange transition-transform duration-300 group-hover:-translate-y-1"><Icon size={21} /></span><div><h3 className="text-xl font-bold tracking-tight text-zolix-dark">{title}</h3><p className="mt-3 text-[15px] font-medium leading-relaxed text-zolix-dark/55">{desc}</p></div></div></motion.article>)}</div></div></section>

    <section data-nav-theme="beige" className="bg-zolix-beige px-6 py-24 md:py-32"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-[.75fr_1.25fr]"><div><p className="text-[10px] font-bold uppercase tracking-[0.22em] text-zolix-orange">Multi-Cloud GPU</p><Zap size={34} className="mt-8 text-zolix-orange" /></div><p className="max-w-4xl text-2xl font-medium leading-relaxed tracking-tight text-zolix-dark md:text-4xl">Most teams don&apos;t run GPU workloads on a single provider, and GPU pricing varies significantly across AWS, Azure, GCP, and specialized GPU cloud providers for the same hardware class. Zolix&apos;s GPU Calculator gives real-time cross-cloud pricing comparisons, so a training run or inference deployment decision starts with actual current pricing data, not a stale internal spreadsheet.</p></div></section>

    <section data-nav-theme="dark" className="bg-zolix-dark px-6 py-20 text-white md:py-24"><div className="mx-auto max-w-7xl"><p className="mb-10 text-[10px] font-bold uppercase tracking-[0.22em] text-zolix-orange">What teams see after connecting GPU infrastructure to Zolix</p><div className="grid gap-10 border-t border-white/15 pt-9 md:grid-cols-3"><div><p className="text-3xl font-extrabold tracking-[-0.06em] text-zolix-orange md:text-4xl">24 Hours</p><p className="mt-4 text-sm font-medium leading-relaxed text-white/60">to a first cost visibility report across every connected GPU-backed environment.</p></div><div><p className="text-3xl font-extrabold tracking-[-0.06em] text-zolix-orange md:text-4xl">Training/Inference Split</p><p className="mt-4 text-sm font-medium leading-relaxed text-white/60">separate cost lines from day one, instead of one blended GPU total.</p></div><div><p className="text-3xl font-extrabold tracking-[-0.06em] text-zolix-orange md:text-4xl">Zero Agents</p><p className="mt-4 text-sm font-medium leading-relaxed text-white/60">Read-only access - Zolix connects through read-only IAM roles, with no agents installed inside training or inference environments.</p></div></div></div></section>

    <FAQ items={faqs} theme="beige" />

    <section data-nav-theme="dark" className="bg-zolix-dark px-6 py-20 text-white md:py-28"><div className="mx-auto max-w-5xl text-center"><ShieldCheck size={28} className="mx-auto text-zolix-orange" /><h2 className="mt-8 text-4xl font-bold leading-none tracking-[-0.05em] md:text-6xl">Stop Paying for GPU Capacity You Never Touch</h2><p className="mx-auto mt-7 max-w-2xl text-lg font-medium leading-relaxed text-white/60">Connect your GPU infrastructure through read-only access and see exactly which instances are idle, which are mismatched to their workload, and where committed pricing would actually pay off.</p><div className="mt-10 flex flex-wrap justify-center gap-4"><Button href="https://lite.zolix.ai/signup" light>Get Started</Button><Button href="/demo" light>Book a Demo</Button></div></div></section>
  </>;
}
